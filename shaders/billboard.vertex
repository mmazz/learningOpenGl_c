#version 330 core
layout (location = 0) in vec4 aPosRadius; // xyz + radius
uniform mat4 view;
uniform mat4 projection;

void main() {
    gl_Position = projection * view * vec4(aPosRadius.xyz, 1.0);
    gl_PointSize = aPosRadius.w * 100.0 / gl_Position.w; // escala con distancia
}

